#include <stdio.h>


// C.U. : Le premier caractère ne correspond pas à -1 de pas au caractère de fin de code.
main
(void)
{
  char c;
  char cle;
  printf ("clef de chiffrement ?\n");
  cle = getchar();
  printf ("entrez le texte\n");
  do
    {
      c=getchar();
      c= c ^cle;
      putchar(c);
    }while (!(c==-1));

    return 0;
 
    }
// Je ne sais pas comment faire pour ne pas retourner le retour chariot.
