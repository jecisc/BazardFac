(* Ferlicot-Delbecque Cyril
TP10 *)

	(* Question 3 *)

(* 
Quand on appel la fonction avec p < 0 et p > 1 on reçois une exception "Canal_binaire.Pas_une_proba p".
Quand on appel la fonction avec n < 0 et n > 256 on reçois une exception "Canal_binaire.Pas_un_octet n".
Quand p = 0 la valeur renvoyé est l'octet codant l'int que l'on a rentré.
Quand p = 1 la valeur renvoyé est: inverse(octet codant). Donc si l'octet est 10011011 on aura 01100100
*)

	(* Question 7 *)
	
(* Tout dépend de la chance mais le seuille se situe entre 0.02 et 0.05. Entre ces deux valeurs il y a de fortes chance que le fichier devienne méconniassable. *)

	(* Question 8*)

(* Lorsque l'on essaye on se retrouve avec ue erreur lors de la décompression de l'archive *)

	(* Question 12 *)
	
(* Maintenant ça reste lisible jusqu'à environ p = 0.1 *)